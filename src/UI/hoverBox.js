import React from 'react';
import classes from './hoverBox.module.css';
const HoverBox = props => {

  return (
    <>
      {/* <rect className={classes.hoverBoxRect} x={props.pos[0]} y={props.pos[1]}  height="20"></rect> */}
      {/* <text  className={classes.hoverBox} x={props.pos[0]} y={props.pos[1]} fontSize="0.7rem" fill="black" transform={props.transform}>
        {props.text}
      </text> */}
    </>
  );
};

export default HoverBox;
